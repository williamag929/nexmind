# NexMind — Environment Variables
# Copy this file to .env and fill in your values before running docker compose

# ── Claude AI ──────────────────────────────────────────────────────────────────
# Get your API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...

# Claude model used for chat, extraction, and document analysis
CLAUDE_MODEL=claude-sonnet-4-20250514

# ── Authentication ─────────────────────────────────────────────────────────────
# JWT secret for signing session tokens. REQUIRED for production.
# Generate with: openssl rand -hex 64
JWT_SECRET=change_me_jwt_secret_64chars

# JWT token expiration (default: 7d)
JWT_EXPIRES_IN=7d

# Legacy API token (optional) — allows admin/service access without user login.
# If set, requests with this bearer token bypass user auth.
API_TOKEN=

# ── Nextcloud Admin Credentials ────────────────────────────────────────────────
# These are set on first Nextcloud initialization
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=change_me_nextcloud_pass

# ── MariaDB (Nextcloud database) ───────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=change_me_root_pass
MYSQL_PASSWORD=change_me_mysql_pass

# ── Webhook Security ───────────────────────────────────────────────────────────
# Shared secret between Nextcloud Flow and NexMind — prevents unauthorized calls.
# Generate a random value with: openssl rand -hex 32
WEBHOOK_SECRET=change_me_webhook_secret_32chars
